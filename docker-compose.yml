services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: microservice
      POSTGRES_USER: root
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
